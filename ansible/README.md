# paxos postgres Ansibles

## How to run a playbook using ansible
```bash
ansible-playbook -v -i <inventory_file> <playbook_file>
```

### Steps
1. Run Server to create partitions:
2. Run client to send transactions:

## What It will create
### Server
